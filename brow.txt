const socket = new WebSocket('ws://localhost:8081');

socket.onmessage = (event) => {
    console.log('mesg from server: ', event.data);

}

socket.onopen = () => {

    socket.send('hello from client'); 

}